# Projects

Place HTML dashboard files here, organized by project.

Each dashboard should be a standalone HTML file generated from Python
(e.g., using `plotly`, `dash`, `altair`, or similar tools).

After adding files, update `../index.html` with a link to each new dashboard.
