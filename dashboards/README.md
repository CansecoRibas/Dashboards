# Dashboards

Place HTML dashboard files here.

Each dashboard should be a standalone HTML file generated from R
(e.g., via `rmarkdown::render()`, `flexdashboard`, or `htmlwidgets`).

After adding files, update `../index.html` with a link to each new dashboard.
